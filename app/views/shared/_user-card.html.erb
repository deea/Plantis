<a href="/user_plants?user=<%= user.id %>">
  <div class="card-user user-card">
    <div class="avatar-container">
      <span><%= render './shared/user-avatar-large', user: user %></span>
      <div class="user-card-product-infos">
        <div class="user-name">
          <h3><small><%= user.firstname %> <%= user.lastname %></small></h3>
        </div>
      </div>
    </div>
    <div class="follow-margin">
      <% if current_user.following %>
        <%= link_to "Follow", follow_user_path(user), :method => :post, class: "btn btn-primary"  %>
      <% end %>
    </div>
  </div>
</a>
