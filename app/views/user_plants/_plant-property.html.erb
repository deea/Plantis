<div class="card">
  <div class="card-header plant-property-header" id="heading<%= property %>">
    <h2 class="mb-0">
      <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapse<%= property %>" aria-expanded="false" aria-controls="collapse<%= property %>">
        <%= property %>
      </button>
    </h2>
  </div>
  <div id="collapse<%= property %>" class="collapse" aria-labelledby="heading<%= property %>" data-parent="#plantProperties">
    <div class="card-body">
      <%= info %>
    </div>
  </div>
</div>