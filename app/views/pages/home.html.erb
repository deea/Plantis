<body>
  <div class="main-container">
    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.1)), url(<%= image_path "banner4.jpg" %>);">
      <div class="home-header">
        <h1><strong>Houseplant killer?</strong></h1>
        <p>Plantis helps you look after your house plants by reminding you</p>
        <p>when each plant is thirsty.</p>
        <% if user_signed_in? && current_user.user_plants.count > 0 %>
          <%= link_to 'Visit your jungle', user_plants_path, class: 'btn btn-primary btn-banner' %>
          <%= link_to 'Add to your jungle', plants_path, class: 'btn btn-primary btn-banner' %>
        <% elsif user_signed_in? && current_user.user_plants.count == 0 %>
          <%= link_to 'Start your jungle', plants_path, class: 'btn btn-primary btn-banner' %>
        <% else %>
          <%= link_to 'Start your jungle', new_user_registration_path, class: 'btn btn-primary btn-banner' %>
        <% end%>
      </div>
    </div>
    <div class="how-it-works">
      <h2>How it works</h2>
        <div class="cards">
          <div class="card" style="background-image: url(<%= image_path "request.png" %> )">
            <h4>Create your Jungle</h4>
            <br>
            <br>
            <p>Sign up and start adding your plants to your personal jungle</p>
          </div>
          <div class="card" style="background-image: url(<%= image_path "browse.png" %> )">
            <h4>Set goals</h4>
            <br>
            <br>
            <p>Set goals and keep your plants alive by following your watering calendar</p>
          </div>
          <div class="card" style="background-image: url(<%= image_path "delivery.png" %> )">
            <h4>Earn rewards</h4>
            <br>
            <br>
            <p>Once your task completed, you'll earn seeds that could get you rewards</p>
          </div>
        </div>
    </div>
  </div>
</body>
