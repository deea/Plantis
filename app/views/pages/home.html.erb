<body>
  <div>
    <div class="banner">
      <div class="home-header">
        <h1><strong>Houseplant killer?</strong></h1>
        <p>Plantis helps you look after your house plants by reminding you</p>
        <p>when each plant is thirsty.</p>
        <% if user_signed_in? && current_user.user_plants.count > 0 %>
          <%= link_to 'Visit your jungle', user_plants_path, class: 'btn btn-primary btn-banner' %>
          <%= link_to 'Add to your jungle', plants_path, class: 'btn btn-primary btn-banner' %>
        <% elsif user_signed_in? && current_user.user_plants.count == 0 %>
          <%= link_to 'Start your jungle', plants_path, class: 'btn btn-primary btn-banner' %>
        <% else %>
          <%= link_to 'Start your jungle', new_user_registration_path, class: 'btn btn-primary btn-banner' %>
        <% end%>
      </div>
    </div>
    <div class="how-it-works">
      <h2>How it works</h2>
        <div class="home-cards">
          <div class="home-card">
            <h3>Create your Jungle</h3>
            <%= image_tag "working_with_computer.svg", class: "home-image" %>
            <h6>Sign up and start adding plants to your personal jungle</h6>
          </div>

          <div class="home-card">
            <h3>Set goals</h3>
            <%= image_tag "calendar.svg", class: "home-image"  %>
            <h6>Keep your plants alive by following your watering calendar</h6>
          </div>

          <div class="home-card">
            <h3>Earn rewards</h3>
            <%= image_tag "social_media_1.svg", class: "home-image"  %>
            <h6>Earn seeds and socialize with your fellow gardeners</h6>
          </div>
        </div>
    </div>
  </div>
</body>
